Resources:
  UdemyCourseDB:
    Type: AWS::RDS::DBInstance
    DeletionPolicy: Snapshot
    UpdateReplacePolicy: Snapshot
    Properties:
      DBInstanceIdentifier: udemy-course-${opt:stage,'dev'}
      DBName: tcUdemyCourse
      DBInstanceClass: db.t4g.small
      VPCSecurityGroups:
        - sg-c31b46a6
      DBSubnetGroupName: mysql-db-subnet
      AllocatedStorage: 20
      Engine: postgres
      PubliclyAccessible: false
      # Existing unencrypted instances require a snapshot/restore replacement.
      StorageEncrypted: true
      MasterUsername: ${self:custom.UDEMY_CRS_PG_USER}
      MasterUserPassword: ${self:custom.UDEMY_CRS_PG_PASSWORD}
